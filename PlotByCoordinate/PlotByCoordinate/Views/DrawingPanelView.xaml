<Window
    x:Class="PlotByCoordinate.View.DrawingPanelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cov="clr-namespace:PlotByCoordinate.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:local="clr-namespace:PlotByCoordinate.View"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="http://www.galasoft.ch/mvvmlight"
    x:Name="win"
    Title="DrawingPanelView"
    Width="800"
    Height="600"
    DataContext="{Binding Path=Corrdinate, Source={StaticResource Locator}}"
    mc:Ignorable="d">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="SizeChanged">
            <mvvm:EventToCommand Command="{Binding SizeChangedCommand}" PassEventArgsToCommand="True" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Style.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Window.InputBindings>
        <KeyBinding Modifiers="Ctrl" Key="B" Command= "{Binding KeyDownCommand}" />
    </Window.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="11*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Border Grid.Row="0" Background="#3448a1" />
        <Border
            Grid.Row="0"
            Grid.Column="1"
            Background="#3448a1" />
        <Border
            Grid.Row="1"
            Grid.Column="0"
            Background="#000000" />
        <Border
            Grid.Row="1"
            Grid.Column="1"
            Background="#f1f1f1" />
        <StackPanel
            Grid.Row="1"
            Grid.Column="1"
            VerticalAlignment="Center">
            <StackPanel x:Name="anglepanel">
                <Label HorizontalAlignment="Center" Content="三角形" />
                <Label HorizontalAlignment="Left" Content="P1" />
                <DockPanel>
                    <TextBox
                        Name="x1"
                        DockPanel.Dock="Left"
                        Style="{DynamicResource ResourceKey=TextBoxStyle}"
                        Text="{Binding CoordinateValue.TriangleX1, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox
                        Name="y1"
                        DockPanel.Dock="Right"
                        Style="{DynamicResource ResourceKey=TextBoxStyle}"
                        Text="{Binding CoordinateValue.TriangleY1, UpdateSourceTrigger=PropertyChanged}" />
                </DockPanel>
                <Label HorizontalAlignment="Left" Content="P2" />
                <DockPanel>
                    <TextBox
                        Name="x2"
                        DockPanel.Dock="Left"
                        Style="{DynamicResource ResourceKey=TextBoxStyle}"
                        Text="{Binding CoordinateValue.TriangleX2, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox
                        Name="y2"
                        DockPanel.Dock="Right"
                        Style="{DynamicResource ResourceKey=TextBoxStyle}"
                        Text="{Binding CoordinateValue.TriangleY2, UpdateSourceTrigger=PropertyChanged}" />
                </DockPanel>
                <Label HorizontalAlignment="Left" Content="P3" />
                <DockPanel>
                    <TextBox
                        Name="x3"
                        DockPanel.Dock="Left"
                        Style="{DynamicResource ResourceKey=TextBoxStyle}"
                        Text="{Binding CoordinateValue.TriangleX3, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox
                        Name="ty3"
                        DockPanel.Dock="Right"
                        Style="{DynamicResource ResourceKey=TextBoxStyle}"
                        Text="{Binding CoordinateValue.TriangleY3, UpdateSourceTrigger=PropertyChanged}">
                    </TextBox>
                </DockPanel>
            </StackPanel>
            <StackPanel x:Name="linepanel">
                <Label HorizontalAlignment="Center" Content="直线" />
                <Label HorizontalAlignment="Left" Content="P1" />
                <DockPanel>
                    <TextBox
                        Name="lx1"
                        DockPanel.Dock="Left"
                        Style="{DynamicResource ResourceKey=TextBoxStyle}"
                        Text="{Binding CoordinateValue.LineX1, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox
                        Name="ly1"
                        DockPanel.Dock="Right"
                        Style="{DynamicResource ResourceKey=TextBoxStyle}"
                        Text="{Binding CoordinateValue.LineY1, UpdateSourceTrigger=PropertyChanged}" />
                </DockPanel>
                <Label HorizontalAlignment="Left" Content="P2" />
                <DockPanel>
                    <TextBox
                        Name="lx2"
                        DockPanel.Dock="Left"
                        Style="{DynamicResource ResourceKey=TextBoxStyle}"
                        Text="{Binding CoordinateValue.LineX2, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox
                        Name="ly2"
                        DockPanel.Dock="Right"
                        Style="{DynamicResource ResourceKey=TextBoxStyle}"
                        Text="{Binding CoordinateValue.LineY2, UpdateSourceTrigger=PropertyChanged}">
                    </TextBox>
                </DockPanel>
            </StackPanel>
        </StackPanel>
        <Canvas
            x:Name="canvas"
            Grid.Row="1"
            Grid.Column="0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Style="{DynamicResource ResourceKey=CanvasStyle}">
            <Path
                x:Name="pathangle"
                Canvas.Left="{Binding CoordinateValue.TriangleXPos}"
                Canvas.Top="{Binding CoordinateValue.TriangleYPos}"
                Style="{DynamicResource ResourceKey=PathStyle}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonDown">
                        <mvvm:EventToCommand Command="{Binding MouseLeftButtonDownCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseLeftButtonUp">
                        <mvvm:EventToCommand Command="{Binding MouseLeftButtonUpCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseMove">
                        <mvvm:EventToCommand Command="{Binding MouseMoveCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <Path.Data>
                    <GeometryGroup>
                        <LineGeometry
                            x:Name="elips"
                            StartPoint="{Binding CoordinateValue.Points[0]}"
                            EndPoint="{Binding CoordinateValue.Points[1]}" />
                        <LineGeometry
                            x:Name="elips1"
                            StartPoint="{Binding CoordinateValue.Points[1]}"
                            EndPoint="{Binding CoordinateValue.Points[2]}" />
                        <LineGeometry
                            x:Name="elips2"
                            StartPoint="{Binding CoordinateValue.Points[2]}"
                            EndPoint="{Binding CoordinateValue.Points[0]}" />
                    </GeometryGroup>
                </Path.Data>
            </Path>
            <Path
                x:Name="pathline"
                Canvas.Left="{Binding CoordinateValue.LineXPos}"
                Canvas.Top="{Binding CoordinateValue.LineYPos}"
                Style="{DynamicResource ResourceKey=PathStyle}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonDown">
                        <mvvm:EventToCommand Command="{Binding MouseLeftButtonDownCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseLeftButtonUp">
                        <mvvm:EventToCommand Command="{Binding MouseLeftButtonUpCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseMove">
                        <mvvm:EventToCommand Command="{Binding MouseMoveCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <Path.Data>
                    <LineGeometry
                        x:Name="line"
                        StartPoint="{Binding CoordinateValue.Points[3], UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                        EndPoint="{Binding CoordinateValue.Points[4], UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
                </Path.Data>
            </Path>
        </Canvas>
    </Grid>
</Window>
