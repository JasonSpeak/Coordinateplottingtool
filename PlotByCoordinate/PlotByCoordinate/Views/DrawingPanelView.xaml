<Window
    x:Class="PlotByCoordinate.View.DrawingPanelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cov="clr-namespace:PlotByCoordinate.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:local="clr-namespace:PlotByCoordinate.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="http://www.galasoft.ch/mvvmlight"
    Title="DrawingPanelView"
    Width="800"
    Height="600"
    DataContext="{Binding Path=Corrdinate, Source={StaticResource Locator}}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="11*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Border Grid.Row="0" Background="#3448a1" />
        <Border
            Grid.Row="0"
            Grid.Column="1"
            Background="#3448a1" />
        <Border
            Grid.Row="1"
            Grid.Column="0"
            Background="#000000" />
        <Border
            Grid.Row="1"
            Grid.Column="1"
            Background="#f1f1f1" />
        <StackPanel
            Grid.Row="1"
            Grid.Column="1"
            VerticalAlignment="Center">
            <StackPanel x:Name="anglepanel">
                <Label HorizontalAlignment="Center" Content="三角形" />
                <Label HorizontalAlignment="Left" Content="P1" />
                <DockPanel>
                    <TextBox
                        Name="x1"
                        Width="60"
                        Margin="3"
                        Background="LightBlue"
                        DockPanel.Dock="Left"
                        Text="{Binding CoordinateValue.TriangleX1}" />
                    <TextBox
                        Name="y1"
                        Width="60"
                        Margin="3"
                        Background="LightBlue"
                        DockPanel.Dock="Right"
                        Text="{Binding CoordinateValue.TriangleY1}" />
                </DockPanel>
                <Label HorizontalAlignment="Left" Content="P2" />
                <DockPanel>
                    <TextBox
                        Name="x2"
                        Width="60"
                        Margin="3"
                        Background="LightBlue"
                        DockPanel.Dock="Left"
                        Text="{Binding CoordinateValue.TriangleX2}" />
                    <TextBox
                        Name="y2"
                        Width="60"
                        Margin="3"
                        Background="LightBlue"
                        DockPanel.Dock="Right"
                        Text="{Binding CoordinateValue.TriangleY2}" />
                </DockPanel>
                <Label HorizontalAlignment="Left" Content="P3" />
                <DockPanel>
                    <TextBox
                        Name="x3"
                        Width="60"
                        Margin="3"
                        Background="LightBlue"
                        DockPanel.Dock="Left"
                        Text="{Binding CoordinateValue.TriangleX3}" />
                    <TextBox
                        Name="ty3"
                        Width="60"
                        Margin="3"
                        Background="LightBlue"
                        DockPanel.Dock="Right"
                        Text="{Binding CoordinateValue.TriangleY3}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="LostFocus">
                                <mvvm:EventToCommand Command="{Binding LostFocusCommand}" PassEventArgsToCommand="True" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </TextBox>
                </DockPanel>
            </StackPanel>
            <StackPanel x:Name="linepanel">
                <Label HorizontalAlignment="Center" Content="直线" />
                <Label HorizontalAlignment="Left" Content="P1" />
                <DockPanel>
                    <TextBox
                        Name="lx1"
                        Width="60"
                        Margin="3"
                        Background="LightBlue"
                        DockPanel.Dock="Left"
                        Text="{Binding CoordinateValue.LineX1}" />
                    <TextBox
                        Name="ly1"
                        Width="60"
                        Margin="3"
                        Background="LightBlue"
                        DockPanel.Dock="Right"
                        Text="{Binding CoordinateValue.LineY1}" />
                </DockPanel>
                <Label HorizontalAlignment="Left" Content="P2" />
                <DockPanel>
                    <TextBox
                        Name="lx2"
                        Width="60"
                        Margin="3"
                        Background="LightBlue"
                        DockPanel.Dock="Left"
                        Text="{Binding CoordinateValue.LineX2}" />
                    <TextBox
                        Name="ly2"
                        Width="60"
                        Margin="3"
                        Background="LightBlue"
                        DockPanel.Dock="Right"
                        Text="{Binding CoordinateValue.LineY2}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="LostFocus">
                                <mvvm:EventToCommand Command="{Binding LostFocusCommand}" PassEventArgsToCommand="True" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </TextBox>
                </DockPanel>
            </StackPanel>
        </StackPanel>
        <Canvas
            x:Name="canvas"
            Grid.Row="1"
            Grid.Column="0"
            Margin="0,0,0,0"
            ClipToBounds="True">
            <Path
                x:Name="pathangle"
                Canvas.Left="{Binding CoordinateValue.TriangleXPos}"
                Canvas.Top="{Binding CoordinateValue.TriangleYPos}"
                Stroke="White"
                StrokeThickness="2"
                Visibility="{Binding CoordinateValue.CanSee}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonDown">
                        <mvvm:EventToCommand Command="{Binding MouseLeftButtonDownCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseLeftButtonUp">
                        <mvvm:EventToCommand Command="{Binding MouseLeftButtonUpCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseMove">
                        <mvvm:EventToCommand Command="{Binding MouseMoveCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <Path.Data>
                    <GeometryGroup>
                        <LineGeometry
                            x:Name="elips"
                            StartPoint="{Binding CoordinateValue.Points[0]}"
                            EndPoint="{Binding CoordinateValue.Points[1]}" />
                        <LineGeometry
                            x:Name="elips1"
                            StartPoint="{Binding CoordinateValue.Points[1]}"
                            EndPoint="{Binding CoordinateValue.Points[2]}" />
                        <LineGeometry
                            x:Name="elips2"
                            StartPoint="{Binding CoordinateValue.Points[2]}"
                            EndPoint="{Binding CoordinateValue.Points[0]}" />
                    </GeometryGroup>
                </Path.Data>
            </Path>
            <Path
                x:Name="pathline"
                Canvas.Left="{Binding CoordinateValue.LineXPos}"
                Canvas.Top="{Binding CoordinateValue.LineYPos}"
                Stroke="White"
                StrokeThickness="2">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonDown">
                        <mvvm:EventToCommand Command="{Binding MouseLeftButtonDownCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseLeftButtonUp">
                        <mvvm:EventToCommand Command="{Binding MouseLeftButtonUpCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseMove">
                        <mvvm:EventToCommand Command="{Binding MouseMoveCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <Path.Data>
                    <LineGeometry
                        x:Name="line"
                        StartPoint="{Binding CoordinateValue.Points[3], UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                        EndPoint="{Binding CoordinateValue.Points[4], UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
                </Path.Data>
            </Path>
        </Canvas>
    </Grid>
</Window>
